---
okf_version: "0.2"
---

# My Knowledge Bundle

<!--
The bundle root. The `okf_version` line above is what switches OKF read
semantics on under OKF_MODE=auto — keep it, and keep it alone: an index file
carries no other frontmatter, and `okf_validate` reports one that does. The
heading names the bundle; rename it freely. This listing is navigation, not
content; regenerate it any time with `okf_generate_index` (or let the enforced
write layer refresh it for you when OKF_WRITE is on). If your vault sets
MARKDOWN_VAULT_MCP_REQUIRED_FIELDS, the server adds those fields here when it
regenerates the listing, because the index gate needs them.
-->

- [A concept](/concepts/a-concept.md) - short description
- subfolder/ → [subfolder/index.md](/subfolder/index.md)
